module.exports = {
    /**
     * 4个缩进
     * @default 4
     */
    tabWidth: 4,

    /**
     * 超过 100 个字符换行
     * @default 100
     */
    printWidth: 100,

    /**
     * 使用单引号代替双引号
     * @default false
     */
    singleQuote: true,

    /**
     * 在对象或数组最后一个元素后面是否加逗号
     * @default es5
     */
    trailingComma: 'all',

    /**
     * 箭头函数参数尽可能省略括号
     */
    arrowParens: 'avoid',
};
